# Single-Page Web App

`index.html` contains the PDF workspace, styles, scripts and PDF libraries in one file.

## Run

Open `index.html` directly, or serve the directory from a static web server. This edition does not register a service worker and is not installable as a PWA.
